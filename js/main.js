// При помощи рекурсии найти сумму квадратов массива let arr = [1, 2, 3, 4, 5];

let arr = [1, 2, 3, 4, 5];


let sum = arr.map(i => x += i * i, x = 0).reverse()[0]
console.log(sum)

let dzNum = 191;

let rez = dzNum.toString().split('').map(Number).map(i => e += i, e = 0).reverse()[0]

console.log(rez)